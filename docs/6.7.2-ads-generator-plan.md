# Fase 6.7.2 — Ads Static and Carousel Ads Generator: Implementation Plan

> **Goal:** Generate ad creatives for paid campaigns (static single-image + multi-slide carousel ads) from Brand DNA + a content idea or campaign objective. Ads must be **distinct from organic carousels** — they carry ad-specific fields (headline, primary text, CTA button, destination URL), ad template structures (problem/solution, offer, proof, comparison, testimonial), platform-specific format constraints (Meta, LinkedIn), and ad policy compliance checks.

---

## 0. Design Principles

| Principle | Rationale |
|-----------|-----------|
| **Ads ≠ Organic carousels renamed** | Ads have a fundamentally different structure: a `headline` (125 chars), `primary text` (125 chars for feed), `description`, `CTA button` (Learn More, Shop Now, Sign Up), and `destination URL`. Organic carousels have slide-by-slide narrative content. |
| **Data-driven templates** | Ad templates (offer, proof, comparison, testimonial, problem/solution) are defined as data objects in a `ad-template-definitions.ts` file — same pattern as `carouselTemplateDefinitions`. |
| **Reuse existing infrastructure** | Uses `GenerationJob`, `BrandProfileService`, `ContentIdeaService`, OpenAI structured output, and `validateAiResponse` — no new DB models needed for the core flow. |
| **New `AdCreative` model** | Unlike social posts (which reused the `Post` model), ad creatives have a fundamentally different schema (headline, primary text, CTA, URL, ad format). A dedicated `AdCreative` model in Prisma is warranted. |
| **Platform-aware** | Meta (Facebook/Instagram) and LinkedIn have different ad formats, character limits, and CTA options. The system generates platform-specific ad creatives. |

---

## 1. Prisma Schema Changes

### File: `libraries/nestjs-libraries/src/database/prisma/schema.prisma`

#### 1a. Add `AD_CREATIVE_GENERATION` to `GenerationJobType` enum (line ~1142)

```prisma
enum GenerationJobType {
  BRAND_DNA_EXTRACTION
  IDEA_GENERATION
  CAROUSEL_PLAN
  IMAGE_GENERATION
  CAPTION_GENERATION
  BULK_GENERATION
  SOCIAL_POST_GENERATION
  AD_CREATIVE_GENERATION  // NEW
}
```

#### 1b. Add `AdCreative` model (new model, after `Post` model ~line 455)

```prisma
enum AdCreativeType {
  STATIC
  CAROUSEL
}

enum AdObjective {
  AWARENESS
  CONSIDERATION
  CONVERSION
  LEAD_GENERATION
  TRAFFIC
  ENGAGEMENT
}

enum AdPlatform {
  META_FACEBOOK
  META_INSTAGRAM
  LINKEDIN
}

model AdCreative {
  id               String            @id @default(uuid())
  organizationId   String
  brandProfileId   String
  contentIdeaId    String?
  carouselProjectId String?

  // Ad metadata
  type             AdCreativeType
  platform         AdPlatform
  objective        AdObjective
  adTemplateId     String?           // Reference to the ad template used (problem-solution, offer, etc.)

  // Ad copy fields
  headline         String            // Max 125 chars (feed), 40 chars (stories)
  primaryText      String            // Main ad body text
  description      String?           // Link description (Meta) / intro text (LinkedIn)
  ctaButton        String            // e.g. "LEARN_MORE", "SHOP_NOW", "SIGN_UP"
  destinationUrl   String?

  // Carousel-specific (null for static ads)
  slides           Json?             // Array of { headline, body, imageUrl, cta? } for carousel ads
  slideCount       Int?

  // Ad policy / compliance
  policyWarnings   Json?             // Array of { severity, ruleId, message, suggestion }
  claimsFlags      Json?             // Array of { claim, severity, category }

  // Image references
  mediaAssets      Json?             // Array of { url, mediaId, altText, role } for generated/uploaded images

  // Generation tracking
  generationJobId  String?
  schemaVersion    String?
  promptVersion    String?

  // Status
  status           String            @default("DRAFT")  // DRAFT, APPROVED, EXPORTED

  createdAt        DateTime          @default(now())
  updatedAt        DateTime          @updatedAt
  deletedAt        DateTime?

  organization     Organization      @relation(fields: [organizationId], references: [id])
  brandProfile     BrandProfile      @relation(fields: [brandProfileId], references: [id])
  contentIdea      ContentIdea?      @relation(fields: [contentIdeaId], references: [id])
  carouselProject  CarouselProject?  @relation(fields: [carouselProjectId], references: [id])

  @@index([organizationId])
  @@index([brandProfileId])
  @@index([contentIdeaId])
  @@index([carouselProjectId])
  @@index([platform])
  @@index([type])
  @@index([status])
  @@index([createdAt])
}
```

#### 1c. Add relations to existing models

In `ContentIdea` model (after line ~1080):
```prisma
model ContentIdea {
  // ... existing fields ...
  adCreatives     AdCreative[]
}
```

In `CarouselProject` model:
```prisma
model CarouselProject {
  // ... existing fields ...
  adCreatives     AdCreative[]
}
```

In `BrandProfile` model:
```prisma
model BrandProfile {
  // ... existing fields ...
  adCreatives     AdCreative[]
}
```

#### 1d. Migration command

```bash
npx prisma migrate dev --name add-ad-creative-model
```

---

## 2. Zod Schema for Ad Creative Output

### File: `libraries/nestjs-libraries/src/ai-generate/schemas/ad-creative.schema.ts` (NEW)

```typescript
import { z } from 'zod';

export const VERSION = '1.0.0';

// ---- Platform-specific constraints ----

export const AD_PLATFORM_CONSTRAINTS: Record<
  string,
  {
    maxHeadline: number;
    maxPrimaryText: number;
    maxDescription: number;
    supportedCtas: string[];
    maxCarouselSlides: number;
    imageAspectRatios: string[];
  }
> = {
  META_FACEBOOK: {
    maxHeadline: 125,        // Feed: 125, Stories: 40
    maxPrimaryText: 125,     // Short text recommended; max 500
    maxDescription: 30,
    supportedCtas: [
      'LEARN_MORE', 'SHOP_NOW', 'SIGN_UP', 'BOOK_TRAVEL', 'CONTACT_US',
      'DOWNLOAD', 'GET_OFFER', 'SUBSCRIBE', 'WATCH_MORE', 'APPLY_NOW',
      'ORDER_NOW', 'REQUEST_TIME',
    ],
    maxCarouselSlides: 10,
    imageAspectRatios: ['1:1', '4:5', '16:9'],
  },
  META_INSTAGRAM: {
    maxHeadline: 125,
    maxPrimaryText: 125,
    maxDescription: 30,
    supportedCtas: [
      'LEARN_MORE', 'SHOP_NOW', 'SIGN_UP', 'CONTACT_US',
      'DOWNLOAD', 'GET_OFFER', 'WATCH_MORE', 'ORDER_NOW',
    ],
    maxCarouselSlides: 10,
    imageAspectRatios: ['1:1', '4:5'],
  },
  LINKEDIN: {
    maxHeadline: 70,         // LinkedIn is stricter
    maxPrimaryText: 150,     // Short intro text; max 3000
    maxDescription: 100,
    supportedCtas: [
      'LEARN_MORE', 'SIGN_UP', 'DOWNLOAD', 'REQUEST_DEMO',
      'CONTACT_US', 'REGISTER', 'SUBSCRIBE', 'APPLY',
    ],
    maxCarouselSlides: 5,    // LinkedIn carousel ads (single image only for now, carousel via document ads)
    imageAspectRatios: ['1:1', '1.91:1'],
  },
};

// ---- Ad copy sub-schema ----

const AdCopySchema = z.object({
  headline: z
    .string()
    .min(1)
    .max(125)
    .describe('Short, punchy headline for the ad (max 125 chars for Meta feed)'),
  primaryText: z
    .string()
    .min(1)
    .describe('Main ad body text shown above the image'),
  description: z
    .string()
    .optional()
    .describe('Optional description/link preview text'),
  ctaButton: z
    .string()
    .describe('CTA button type (e.g. LEARN_MORE, SHOP_NOW, SIGN_UP)'),
  destinationUrl: z
    .string()
    .url()
    .optional()
    .describe('Destination URL for the ad click'),
});

// ---- Carousel slide sub-schema ----

const AdCarouselSlideSchema = z.object({
  index: z.number().int().nonnegative().describe('Slide position (0-based)'),
  headline: z.string().min(1).max(125).describe('Slide headline'),
  body: z.string().min(1).describe('Slide body text'),
  imageUrl: z.string().url().optional().describe('Image URL for this slide'),
  cta: z.string().optional().describe('Slide-specific CTA override'),
  altText: z.string().optional().describe('Accessibility alt text for the image'),
});

// ---- Policy warning sub-schema ----

const PolicyWarningSchema = z.object({
  ruleId: z.string().describe('Policy rule identifier (e.g. META_HEALTH_CLAIMS)'),
  severity: z.enum(['info', 'warning', 'critical']).describe('Severity level'),
  message: z.string().describe('Human-readable warning message'),
  suggestion: z.string().optional().describe('Suggested fix or alternative'),
  category: z.string().describe('Policy category (e.g. health, finance, before-after)'),
});

const ClaimFlagSchema = z.object({
  claim: z.string().describe('The flagged claim text'),
  severity: z.enum(['info', 'warning', 'critical']),
  category: z.string().describe('Claim category (e.g. health, guaranteed-results, testimonials)'),
  platform: z.string().optional().describe('Platform where this claim is restricted'),
});

// ---- Main Ad Creative schema ----

export const AdCreativeSchema = z.object({
  type: z.enum(['STATIC', 'CAROUSEL']).describe('Ad creative type'),
  platform: z
    .string()
    .describe('Target ad platform (META_FACEBOOK, META_INSTAGRAM, LINKEDIN)'),
  objective: z
    .enum(['AWARENESS', 'CONSIDERATION', 'CONVERSION', 'LEAD_GENERATION', 'TRAFFIC', 'ENGAGEMENT'])
    .describe('Campaign objective'),
  adTemplateId: z
    .string()
    .optional()
    .describe('ID of the ad template used'),

  // Ad copy
  headline: z.string().min(1).max(125).describe('Ad headline'),
  primaryText: z.string().min(1).describe('Primary ad text'),
  description: z.string().optional().describe('Link description'),
  ctaButton: z.string().describe('CTA button type'),
  destinationUrl: z.string().optional().describe('Destination URL'),

  // Carousel (optional)
  slides: z
    .array(AdCarouselSlideSchema)
    .min(2)
    .max(10)
    .optional()
    .describe('Carousel slides (only for CAROUSEL type)'),
  slideCount: z.number().int().optional().describe('Number of slides'),

  // Image prompts (for image generation)
  imagePrompts: z
    .array(z.object({
      role: z.string().describe('Image role (e.g. hero, slide-1, slide-2)'),
      prompt: z.string().describe('Image generation prompt'),
      aspectRatio: z.string().optional().describe('Recommended aspect ratio'),
    }))
    .optional()
    .describe('Prompts for generating images for this ad'),

  // Compliance
  policyWarnings: z.array(PolicyWarningSchema).default([]).describe('Ad policy warnings'),
  claimsFlags: z.array(ClaimFlagSchema).default([]).describe('Flagged claims'),

  // Metadata
  tone: z.string().describe('Tone used for this ad'),
  rationale: z.string().optional().describe('Why this approach was chosen'),
  notes: z.string().optional().describe('Internal notes'),
});

export type AdCreative = z.infer<typeof AdCreativeSchema>;

// ---- Batch schema ----

export const AdCreativeBatchSchema = z.object({
  ads: z
    .array(AdCreativeSchema)
    .min(1)
    .describe('Array of generated ad creatives'),
});

export type AdCreativeBatch = z.infer<typeof AdCreativeBatchSchema>;

/**
 * Validate an unknown payload against the AdCreativeBatch schema.
 */
export function validate(data: unknown): {
  success: boolean;
  data: AdCreativeBatch | null;
  errors: z.ZodError | null;
} {
  const result = AdCreativeBatchSchema.safeParse(data);
  if (result.success) {
    return { success: true, data: result.data, errors: null };
  }
  return { success: false, data: null, errors: result.error };
}

/**
 * Parse an unknown payload against the AdCreativeBatch schema.
 */
export function parse(data: unknown): AdCreativeBatch {
  return AdCreativeBatchSchema.parse(data);
}
```

---

## 3. Ad Template Definitions (Data, Not Hardcode)

### File: `libraries/nestjs-libraries/src/ai-generate/ad-templates/ad-template-definitions.ts` (NEW)

```typescript
/**
 * Ad Template Definitions — ContentFlow Ads Generator v1.0.0
 *
 * Data-driven ad template catalog. Each template defines a narrative
 * structure, ad copy strategy, and policy checks specific to paid advertising.
 *
 * These are NOT organic carousel templates repurposed — they define
 * the problem/solution flow, social proof integration, and CTA strategy
 * specific to conversion-focused ad creatives.
 */

export const AD_TEMPLATE_SCHEMA_VERSION = '1.0.0';

// ---- Types ----

export type AdTemplateSlideRole =
  | 'hook'           // Pattern interrupt / attention grabber
  | 'problem'        // Agitate the pain point
  | 'solution'       // Present the product/service as the answer
  | 'proof'          // Social proof, testimonials, data
  | 'benefit'        // Key benefit highlight
  | 'comparison'     // Before/after or vs competitor
  | 'offer'          // Special offer, discount, urgency
  | 'testimonial'    // Customer testimonial / case study
  | 'cta';           // Final call to action

export type AdTemplateSlideRule = {
  role: AdTemplateSlideRole;
  description: string;
  maxHeadlineChars: number;
  maxBodyChars: number;
  isRequired: boolean;
};

export type AdPolicyCheck = {
  id: string;
  description: string;
  severity: 'info' | 'warning' | 'critical';
  pattern?: string;       // regex as string
  message: string;
  platforms?: string[];   // If null, applies to all platforms
  category: string;       // health, finance, before-after, testimonials, guarantees
};

export type AdTemplateDefinition = {
  id: string;
  label: string;             // e.g. "Problema → Solução"
  labelEn: string;           // English label for API consistency
  description: string;
  version: string;
  active: boolean;
  category: string;          // 'conversion', 'proof', 'offer', 'engagement'
  objective: string[];       // Which ad objectives this template supports
  preferredPlatforms: string[];
  slideStructure: AdTemplateSlideRule[];
  copyStrategy: {
    headlineApproach: string;   // How to write headlines for this template
    primaryTextApproach: string;
    descriptionApproach: string;
    ctaRecommendation: string;
  };
  promptInstruction: string;    // Instructions fed to the AI
  policyChecks: AdPolicyCheck[];
  exampleHeadlines: string[];
  examplePrimaryTexts: string[];
};

// ---- Template Definitions ----

export const adTemplateDefinitions: AdTemplateDefinition[] = [
  // =========================================================================
  // 1. PROBLEMA → SOLUÇÃO (Problem → Solution)
  // =========================================================================
  {
    id: 'problem-solution',
    label: 'Problema → Solução',
    labelEn: 'Problem → Solution',
    description:
      'Ad que identifica uma dor clara do público e apresenta o produto/serviço como a solução direta. Estrutura clássica de conversão.',
    version: '1.0.0',
    active: true,
    category: 'conversion',
    objective: ['CONVERSION', 'TRAFFIC', 'CONSIDERATION'],
    preferredPlatforms: ['META_FACEBOOK', 'META_INSTAGRAM', 'LINKEDIN'],
    slideStructure: [
      { role: 'hook', description: 'Gancho que identifica a dor', maxHeadlineChars: 125, maxBodyChars: 0, isRequired: true },
      { role: 'problem', description: 'Aprofunda o problema/pain point', maxHeadlineChars: 125, maxBodyChars: 150, isRequired: true },
      { role: 'solution', description: 'Apresenta o produto como solução', maxHeadlineChars: 125, maxBodyChars: 150, isRequired: true },
      { role: 'benefit', description: 'Benefício principal resultante', maxHeadlineChars: 125, maxBodyChars: 100, isRequired: true },
      { role: 'cta', description: 'CTA direto com oferta', maxHeadlineChars: 125, maxBodyChars: 80, isRequired: true },
    ],
    copyStrategy: {
      headlineApproach: 'Curto e direto, focado na dor ou na solução. Ex: "Cansado de X? Descubra Y"',
      primaryTextApproach: 'Comece com a dor, depois apresente a solução em 2-3 frases',
      descriptionApproach: 'Reforce o benefício principal em uma linha',
      ctaRecommendation: 'LEARN_MORE ou SIGN_UP — convite suave para descobrir mais',
    },
    promptInstruction:
      'Gere um anúncio na estrutura Problema→Solução. Comece com um gancho que identifique a dor principal do público-alvo. Aprofunde o problema em 1-2 frases. Apresente o produto/serviço como a solução direta. Destaque o benefício principal. Feche com CTA claro.',
    policyChecks: [
      {
        id: 'ps-no-exaggeration',
        description: 'Verifica se o problema não é exagerado de forma alarmista',
        severity: 'warning',
        message: 'Evite linguagem excessivamente alarmista sobre o problema.',
        category: 'tone',
      },
      {
        id: 'ps-solution-evidence',
        description: 'Verifica se a solução tem algum tipo de prova',
        severity: 'info',
        message: 'Considere adicionar prova social ou dados para validar a solução.',
        category: 'proof',
      },
    ],
    exampleHeadlines: ['Cansado de perder tempo com X?', 'O problema que ninguém fala sobre Y'],
    examplePrimaryTexts: [
      'Você sabia que 70% dos profissionais enfrentam esse problema todos os dias? Nossa solução foi projetada para eliminar isso de vez.',
    ],
  },

  // =========================================================================
  // 2. PROVA SOCIAL (Social Proof)
  // =========================================================================
  {
    id: 'social-proof',
    label: 'Prova Social',
    labelEn: 'Social Proof',
    description:
      'Ad centrado em depoimentos, resultados e credibilidade. Usa provas concretas (números, cases, depoimentos) para gerar confiança.',
    version: '1.0.0',
    active: true,
    category: 'proof',
    objective: ['CONSIDERATION', 'CONVERSION', 'TRUST_BUILDING'],
    preferredPlatforms: ['META_FACEBOOK', 'META_INSTAGRAM', 'LINKEDIN'],
    slideStructure: [
      { role: 'hook', description: 'Gancho com dado impressionante ou resultado', maxHeadlineChars: 125, maxBodyChars: 0, isRequired: true },
      { role: 'proof', description: 'Prova concreta (número, case, resultado)', maxHeadlineChars: 125, maxBodyChars: 150, isRequired: true },
      { role: 'testimonial', description: 'Depoimento real ou resultado de cliente', maxHeadlineChars: 125, maxBodyChars: 150, isRequired: true },
      { role: 'benefit', description: 'O que o público pode esperar', maxHeadlineChars: 125, maxBodyChars: 100, isRequired: true },
      { role: 'cta', description: 'CTA com urgência sutil', maxHeadlineChars: 125, maxBodyChars: 80, isRequired: true },
    ],
    copyStrategy: {
      headlineApproach: 'Use números e resultados concretos. Ex: "+500 clientes transformados"',
      primaryTextApproach: 'Comece com o resultado, depois mostre o caminho. Use aspas para depoimentos.',
      descriptionApproach: 'Reforce a credibilidade (ex: "Avaliação 4.9/5 no Google")',
      ctaRecommendation: 'LEARN_MORE ou GET_OFFER — convite baseado em prova',
    },
    promptInstruction:
      'Gere um anúncio centrado em prova social. Comece com um resultado concreto ou dado impressionante. Apresente um case ou depoimento real. Destaque o benefício que o público pode alcançar. Feche com CTA que convida a experimentar.',
    policyChecks: [
      {
        id: 'sp-no-fake-testimonials',
        description: 'Verifica se depoimentos parecem autênticos',
        severity: 'critical',
        message: 'Depoimentos falsos violam políticas do Meta e LinkedIn.',
        category: 'testimonials',
      },
      {
        id: 'sp-no-misleading-numbers',
        description: 'Verifica se números não são enganosos',
        severity: 'warning',
        message: 'Números devem ser verificáveis e contextualizados.',
        category: 'claims',
      },
      {
        id: 'sp-no-before-after-exaggeration',
        description: 'Verifica se antes/depois não é exagerado',
        severity: 'warning',
        pattern: '(antes|depois|transformação completa|100% dos clientes)',
        message: 'Resultados antes/depois não podem ser exagerados ou garantidos.',
        category: 'before-after',
        platforms: ['META_FACEBOOK', 'META_INSTAGRAM'],
      },
    ],
    exampleHeadlines: ['+2.000 empresas já usam nossa solução', 'Resultado comprovado: 40% mais produtividade'],
    examplePrimaryTexts: [
      '"Em 3 meses, triplicamos nossos resultados." — CEO, Empresa X. Veja como você pode fazer o mesmo.',
    ],
  },

  // =========================================================================
  // 3. OFERTA (Offer / Promotion)
  // =========================================================================
  {
    id: 'offer-promotion',
    label: 'Oferta / Promoção',
    labelEn: 'Offer / Promotion',
    description:
      'Ad focado em uma oferta específica com urgência. Ideal para promoções, lançamentos, descontos e campanhas sazonais.',
    version: '1.0.0',
    active: true,
    category: 'offer',
    objective: ['CONVERSION', 'TRAFFIC', 'AWARENESS'],
    preferredPlatforms: ['META_FACEBOOK', 'META_INSTAGRAM', 'LINKEDIN'],
    slideStructure: [
      { role: 'hook', description: 'Gancho com a oferta (desconto, bônus, prazo)', maxHeadlineChars: 125, maxBodyChars: 0, isRequired: true },
      { role: 'benefit', description: 'O que o cliente ganha com a oferta', maxHeadlineChars: 125, maxBodyChars: 120, isRequired: true },
      { role: 'proof', description: 'Prova de que a oferta é valiosa', maxHeadlineChars: 125, maxBodyChars: 100, isRequired: false },
      { role: 'offer', description: 'Detalhes da oferta (preço, desconto, bônus)', maxHeadlineChars: 125, maxBodyChars: 100, isRequired: true },
      { role: 'cta', description: 'CTA urgente com prazo', maxHeadlineChars: 125, maxBodyChars: 80, isRequired: true },
    ],
    copyStrategy: {
      headlineApproach: 'Destaque o desconto ou oferta. Ex: "50% OFF — Últimos 2 dias"',
      primaryTextApproach: 'Comece com a oferta, mostre o valor, crie urgência',
      descriptionApproach: 'Reforce a urgência ou exclusividade',
      ctaRecommendation: 'SHOP_NOW, GET_OFFER ou SIGN_UP — CTA direto à ação',
    },
    promptInstruction:
      'Gere um anúncio de oferta/promoção. Comece com a oferta principal (desconto, bônus, condição especial). Mostre o valor que o cliente recebe. Crie urgência com prazo ou escassez. Feche com CTA de ação imediata.',
    policyChecks: [
      {
        id: 'of-no-fake-urgency',
        description: 'Verifica se urgência é legítima',
        severity: 'warning',
        pattern: '(última chance|acaba hoje|restam apenas \\d+|fim das vagas)',
        message: 'Urgência falsa (falsa escassez) viola políticas de publicidade.',
        category: 'claims',
      },
      {
        id: 'of-no-misleading-pricing',
        description: 'Verifica se preços não são enganosos',
        severity: 'critical',
        message: 'Preços promocionais devem ser reais e verificáveis.',
        category: 'pricing',
      },
      {
        id: 'of-no-fake-discount',
        description: 'Verifica se o desconto parece legítimo',
        severity: 'warning',
        pattern: '(\\d+% OFF|de R\\$.*por R\\$)',
        message: 'Descontos devem ser baseados em preço real de referência.',
        category: 'pricing',
      },
    ],
    exampleHeadlines: ['50% OFF — Só até sexta-feira', 'Plano gratuito por 30 dias'],
    examplePrimaryTexts: [
      'Aproveite nossa maior promoção do ano: 50% de desconto em todos os planos. Oferta válida até 30/06.',
    ],
  },

  // =========================================================================
  // 4. COMPARAÇÃO (Comparison / Before-After)
  // =========================================================================
  {
    id: 'comparison',
    label: 'Comparação / Antes & Depois',
    labelEn: 'Comparison / Before & After',
    description:
      'Ad que compara o "antes" (problema) com o "depois" (solução). Pode incluir comparação com concorrentes ou transformação do cliente.',
    version: '1.0.0',
    active: true,
    category: 'conversion',
    objective: ['CONVERSION', 'CONSIDERATION', 'TRAFFIC'],
    preferredPlatforms: ['META_FACEBOOK', 'META_INSTAGRAM'],
    slideStructure: [
      { role: 'hook', description: 'Gancho: "Você ainda está em X?"', maxHeadlineChars: 125, maxBodyChars: 0, isRequired: true },
      { role: 'problem', description: 'O estado atual (antes) — dor, dificuldade', maxHeadlineChars: 125, maxBodyChars: 120, isRequired: true },
      { role: 'comparison', description: 'O contraste visual/verbal antes vs depois', maxHeadlineChars: 125, maxBodyChars: 120, isRequired: true },
      { role: 'solution', description: 'Como a solução faz a transição', maxHeadlineChars: 125, maxBodyChars: 120, isRequired: true },
      { role: 'cta', description: 'CTA para achieving the result', maxHeadlineChars: 125, maxBodyChars: 80, isRequired: true },
    ],
    copyStrategy: {
      headlineApproach: 'Contraste direto. Ex: "De 0 para 10.000 seguidores em 90 dias"',
      primaryTextApproach: 'Descreva o antes, depois mostre a transformação, cite o caminho',
      descriptionApproach: 'Reforce o resultado alcançável',
      ctaRecommendation: 'LEARN_MORE ou SIGN_UP — convite a experimentar a transformação',
    },
    promptInstruction:
      'Gere um anúncio de comparação/antes-depois. Comece com o estado atual do público (antes). Mostre a transformação possível (depois). Explique como o produto/service faz essa transição. Feche com CTA.',
    policyChecks: [
      {
        id: 'cp-no-misleading-before-after',
        description: 'Verifica se antes/depois não é enganoso',
        severity: 'critical',
        pattern: '(antes.*depois|transformação|de X para Y)',
        message: 'Comparações antes/depois não podem ser enganosas ou irreais.',
        category: 'before-after',
        platforms: ['META_FACEBOOK', 'META_INSTAGRAM'],
      },
      {
        id: 'cp-no-body-shaming',
        description: 'Verifica se não há body-shaming ou manipulação',
        severity: 'critical',
        pattern: '(feio|gordo|magro|barriga|corpo perfeito)',
        message: 'Anúncios não podem usar body-shaming ou manipulação de inseguranças.',
        category: 'sensitive',
      },
      {
        id: 'cp-no-fake-comparison',
        description: 'Verifica se comparação com concorrentes é justa',
        severity: 'warning',
        message: 'Comparações diretas com concorrentes devem ser verificáveis.',
        category: 'claims',
      },
    ],
    exampleHeadlines: ['De 2h por dia para 15 minutos', 'Antes: caótico. Depois: organizado.'],
    examplePrimaryTexts: [
      'Antes: perdendo 2 horas por dia em tarefas manuais. Depois: automatizando tudo em 15 minutos. Veja como.',
    ],
  },

  // =========================================================================
  // 5. DEPOIMENTO / CASE (Testimonial / Case Study)
  // =========================================================================
  {
    id: 'testimonial',
    label: 'Depoimento / Case',
    labelEn: 'Testimonial / Case Study',
    description:
      'Ad construído inteiramente em torno de um depoimento real ou case de sucesso. Coloca o cliente como protagonista e a marca como facilitadora.',
    version: '1.0.0',
    active: true,
    category: 'proof',
    objective: ['CONSIDERATION', 'CONVERSION', 'TRUST_BUILDING'],
    preferredPlatforms: ['META_FACEBOOK', 'META_INSTAGRAM', 'LINKEDIN'],
    slideStructure: [
      { role: 'hook', description: 'Gancho com a frase do depoimento', maxHeadlineChars: 125, maxBodyChars: 0, isRequired: true },
      { role: 'testimonial', description: 'O depoimento completo / contexto', maxHeadlineChars: 125, maxBodyChars: 150, isRequired: true },
      { role: 'proof', description: 'Números ou resultados do case', maxHeadlineChars: 125, maxBodyChars: 120, isRequired: true },
      { role: 'solution', description: 'Como a marca ajudou a alcançar o resultado', maxHeadlineChars: 125, maxBodyChars: 120, isRequired: true },
      { role: 'cta', description: 'CTA para achieving similar results', maxHeadlineChars: 125, maxBodyChars: 80, isRequired: true },
    ],
    copyStrategy: {
      headlineApproach: 'Use a frase mais forte do depoimento como gancho',
      primaryTextApproach: 'Estruture como mini-case: contexto → desafio → solução → resultado',
      descriptionApproach: 'Nome e cargo do cliente para credibilidade',
      ctaRecommendation: 'LEARN_MORE ou REQUEST_DEMO — "Veja como você também pode"',
    },
    promptInstruction:
      'Gere um anúncio baseado em depoimento/case. Comece com a frase mais impactante do depoimento. Forneça contexto sobre quem é o cliente e qual era o desafio. Mostre os resultados alcançados. Explique como a marca ajudou. Feche com CTA convidando a alcançar resultado similar.',
    policyChecks: [
      {
        id: 'tm-no-fake-testimonial',
        description: 'Verifica se depoimento é genuíno',
        severity: 'critical',
        message: 'Depoimentos devem ser de clientes reais. Depoimentos fictíos violam políticas de publicidade.',
        category: 'testimonials',
      },
      {
        id: 'tm-no-unrealistic-results',
        description: 'Verifica se resultados não são irreais',
        severity: 'warning',
        pattern: '(garantimos|resultados garantidos|100% de sucesso)',
        message: 'Resultados não podem ser garantidos. Use "podem variar" ou "resultados típicos".',
        category: 'claims',
      },
      {
        id: 'tm-disclosure-required',
        description: 'Verifica se há disclosure de relationship',
        severity: 'info',
        message: 'Considere adicionar "Resultado individual pode variar" para transparência.',
        category: 'disclosure',
      },
    ],
    exampleHeadlines: ['"Triplicamos o faturamento em 6 meses"', 'Case: Como a Empresa X cresceu 300%'],
    examplePrimaryTexts: [
      '"Antes tínhamos 50 leads/mês. Hoje temos 500. Tudo mudou quando implementamos a solução da Marca Y." — Diretor de Marketing, Empresa X',
    ],
  },
];

// ---- Helper ----

export function getAdTemplateDefinitionById(
  id: string
): AdTemplateDefinition | undefined {
  return adTemplateDefinitions.find((t) => t.id === id);
}

export function getAdTemplatesByObjective(objective: string): AdTemplateDefinition[] {
  return adTemplateDefinitions.filter((t) => t.objective.includes(objective));
}

export function getAdTemplatesByPlatform(platform: string): AdTemplateDefinition[] {
  return adTemplateDefinitions.filter((t) => t.preferredPlatforms.includes(platform));
}
```

### File: `libraries/nestjs-libraries/src/ai-generate/ad-templates/ad-template-registry.ts` (NEW)

```typescript
import {
  adTemplateDefinitions,
  getAdTemplateDefinitionById,
  type AdTemplateDefinition,
} from './ad-template-definitions';

export class AdTemplateRegistry {
  private readonly templates: AdTemplateDefinition[];

  constructor(templates?: AdTemplateDefinition[]) {
    this.templates = templates ?? adTemplateDefinitions;
  }

  get(id: string): AdTemplateDefinition | undefined {
    return this.templates.find((t) => t.id === id);
  }

  require(id: string): AdTemplateDefinition {
    const template = this.get(id);
    if (!template) {
      throw new Error(`Ad template not found: "${id}"`);
    }
    return template;
  }

  getAll(): AdTemplateDefinition[] {
    return [...this.templates];
  }

  getActive(): AdTemplateDefinition[] {
    return this.templates.filter((t) => t.active);
  }

  getByObjective(objective: string): AdTemplateDefinition[] {
    return this.templates.filter((t) => t.objective.includes(objective));
  }

  getByPlatform(platform: string): AdTemplateDefinition[] {
    return this.templates.filter((t) => t.preferredPlatforms.includes(platform));
  }

  getByCategory(category: string): AdTemplateDefinition[] {
    const lower = category.toLowerCase();
    return this.templates.filter((t) => t.category.toLowerCase() === lower);
  }

  getSummary(): Array<{
    id: string;
    label: string;
    labelEn: string;
    description: string;
    category: string;
    objective: string[];
    active: boolean;
    preferredPlatforms: string[];
  }> {
    return this.templates.map((t) => ({
      id: t.id,
      label: t.label,
      labelEn: t.labelEn,
      description: t.description,
      category: t.category,
      objective: t.objective,
      active: t.active,
      preferredPlatforms: t.preferredPlatforms,
    }));
  }

  getIds(): string[] {
    return this.templates.map((t) => t.id);
  }
}

export const adTemplateRegistry = new AdTemplateRegistry();
```

### File: `libraries/nestjs-libraries/src/ai-generate/ad-templates/ad-policy-checker.ts` (NEW)

```typescript
/**
 * Ad Policy Checker — Validates ad creatives against platform policies
 * and brand constraints. Returns warnings and flags for review.
 *
 * This is a post-generation check, not a pre-generation filter.
 * The AI prompt includes policy guidelines, but this checker catches
 * edge cases and provides actionable feedback.
 */

import { type AdCreative } from '../schemas/ad-creative.schema';
import { adTemplateRegistry } from './ad-template-registry';

export type PolicyCheckResult = {
  warnings: PolicyWarning[];
  claimsFlags: ClaimFlag[];
  overallSeverity: 'clean' | 'info' | 'warning' | 'critical';
};

export type PolicyWarning = {
  ruleId: string;
  severity: 'info' | 'warning' | 'critical';
  message: string;
  suggestion?: string;
  category: string;
};

export type ClaimFlag = {
  claim: string;
  severity: 'info' | 'warning' | 'critical';
  category: string;
  platform?: string;
};

// ---- Platform-specific restricted patterns ----

const META_HEALTH_RESTRICTIONS = [
  {
    pattern: /antes\s*(e|\/)\s*depois/i,
    message: 'Meta restringe imagens de antes/depois em anúncios de saúde e beauty.',
    severity: 'critical' as const,
    category: 'before-after',
  },
  {
    pattern: /cura(?:ção|r)?|trata(?:mento)?(?:.*(?:câncer|diabetes|HIV|aids))/i,
    message: 'Meta proíbe claim de cura para condições médicas específicas.',
    severity: 'critical' as const,
    category: 'health',
  },
  {
    pattern: /perda de peso|emagrec(?:er|imento)|gordura/i,
    message: 'Anúncios de perda de peso têm restrições adicionais no Meta.',
    severity: 'warning' as const,
    category: 'health',
  },
];

const GENERIC_RESTRICTIONS = [
  {
    pattern: /garant(?:imos?|ia)|100%\s*(de\s*)?sucesso|resultado\s*garantido/i,
    message: 'Não é permitido garantir resultados em anúncios.',
    severity: 'critical' as const,
    category: 'guarantees',
  },
  {
    pattern: /milagre|revolucionário|sem esforço|sem trabalho/i,
    message: 'Claims de "sem esforço" ou "milagre" são restritos pela maioria das plataformas.',
    severity: 'warning' as const,
    category: 'exaggeration',
  },
  {
    pattern: /último\s*chance|acaba\s*(hoje|agora|já)|restam\s*apenas/i,
    message: 'Urgência falsa (sem prazo real) viola políticas de publicidade.',
    severity: 'warning' as const,
    category: 'urgency',
  },
  {
    pattern: /maior\s*(do mundo|da história|de todos)/i,
    message: 'Claims de superioridade absoluta precisam ser verificáveis.',
    severity: 'warning' as const,
    category: 'superlatives',
  },
  {
    pattern: /secreto|oculto|não\s*(contam|revelam)/i,
    message: 'Marketing de "segredo" pode ser visto como enganoso.',
    severity: 'info' as const,
    category: 'tone',
  },
];

/**
 * Run all policy checks on an ad creative.
 */
export function runAdPolicyChecks(
  ad: AdCreative,
  brandConstraints?: { forbiddenTerms?: string[]; complianceNotes?: string[] }
): PolicyCheckResult {
  const warnings: PolicyWarning[] = [];
  const claimsFlags: ClaimFlag[] = [];

  // 1. Template-specific checks
  if (ad.adTemplateId) {
    const template = adTemplateRegistry.get(ad.adTemplateId);
    if (template) {
      for (const check of template.policyChecks) {
        if (check.pattern) {
          const regex = new RegExp(check.pattern, 'i');
          const textToCheck = `${ad.headline} ${ad.primaryText} ${ad.description || ''}`;
          if (regex.test(textToCheck)) {
            warnings.push({
              ruleId: check.id,
              severity: check.severity,
              message: check.message,
              category: check.category,
            });
          }
        } else {
          // Pattern-less checks are informational
          warnings.push({
            ruleId: check.id,
            severity: check.severity,
            message: check.message,
            category: check.category,
          });
        }
      }
    }
  }

  // 2. Platform-specific checks
  const fullText = `${ad.headline} ${ad.primaryText} ${ad.description || ''}`;

  if (ad.platform === 'META_FACEBOOK' || ad.platform === 'META_INSTAGRAM') {
    for (const restriction of META_HEALTH_RESTRICTIONS) {
      if (restriction.pattern.test(fullText)) {
        claimsFlags.push({
          claim: fullText.match(restriction.pattern)?.[0] || '',
          severity: restriction.severity,
          category: restriction.category,
          platform: ad.platform,
        });
      }
    }
  }

  // 3. Generic restrictions
  for (const restriction of GENERIC_RESTRICTIONS) {
    if (restriction.pattern.test(fullText)) {
      warnings.push({
        ruleId: `GENERIC_${restriction.category.toUpperCase()}`,
        severity: restriction.severity,
        message: restriction.message,
        category: restriction.category,
      });
    }
  }

  // 4. Brand constraint checks
  if (brandConstraints?.forbiddenTerms) {
    for (const term of brandConstraints.forbiddenTerms) {
      if (fullText.toLowerCase().includes(term.toLowerCase())) {
        warnings.push({
          ruleId: `BRAND_FORBIDDEN_${term.toUpperCase().replace(/\s+/g, '_')}`,
          severity: 'warning',
          message: `Termo proibido pela marca encontrado: "${term}"`,
          category: 'brand-constraints',
        });
      }
    }
  }

  // 5. Carousel-specific checks
  if (ad.type === 'CAROUSEL' && ad.slides) {
    if (ad.slides.length < 2) {
      warnings.push({
        ruleId: 'CAROUSEL_MIN_SLIDES',
        severity: 'critical',
        message: 'Carrossel deve ter pelo menos 2 slides.',
        category: 'format',
      });
    }
    if (ad.platform === 'LINKEDIN' && ad.slides.length > 5) {
      warnings.push({
        ruleId: 'LINKEDIN_MAX_SLIDES',
        severity: 'critical',
        message: 'LinkedIn suporta no máximo 5 slides em carrossel.',
        category: 'format',
      });
    }
  }

  // 6. Character limit checks
  const platformLimits: Record<string, { headline: number; primaryText: number }> = {
    META_FACEBOOK: { headline: 125, primaryText: 500 },
    META_INSTAGRAM: { headline: 125, primaryText: 500 },
    LINKEDIN: { headline: 70, primaryText: 3000 },
  };
  const limits = platformLimits[ad.platform];
  if (limits) {
    if (ad.headline.length > limits.headline) {
      warnings.push({
        ruleId: 'HEADLINE_TOO_LONG',
        severity: 'warning',
        message: `Headline excede ${limits.headline} caracteres para ${ad.platform} (${ad.headline.length}/${limits.headline})`,
        category: 'format',
      });
    }
    if (ad.primaryText.length > limits.primaryText) {
      warnings.push({
        ruleId: 'PRIMARY_TEXT_TOO_LONG',
        severity: 'warning',
        message: `Primary text excede ${limits.primaryText} caracteres para ${ad.platform} (${ad.primaryText.length}/${limits.primaryText})`,
        category: 'format',
      });
    }
  }

  // Calculate overall severity
  const hasCritical = warnings.some((w) => w.severity === 'critical') || claimsFlags.some((c) => c.severity === 'critical');
  const hasWarning = warnings.some((w) => w.severity === 'warning') || claimsFlags.some((c) => c.severity === 'warning');
  const hasInfo = warnings.some((w) => w.severity === 'info') || claimsFlags.some((c) => c.severity === 'info');

  const overallSeverity: PolicyCheckResult['overallSeverity'] = hasCritical
    ? 'critical'
    : hasWarning
    ? 'warning'
    : hasInfo
    ? 'info'
    : 'clean';

  return { warnings, claimsFlags, overallSeverity };
}

/**
 * Format policy check results for export/display.
 */
export function formatPolicySummary(result: PolicyCheckResult): string {
  const lines: string[] = [];
  if (result.overallSeverity === 'clean') {
    return '✅ Nenhum problema de compliance detectado.';
  }
  lines.push(`🔍 Compliance: ${result.overallSeverity.toUpperCase()}`);
  lines.push('');
  if (result.warnings.length > 0) {
    lines.push('Warnings:');
    for (const w of result.warnings) {
      const icon = w.severity === 'critical' ? '🚫' : w.severity === 'warning' ? '⚠️' : 'ℹ️';
      lines.push(`  ${icon} [${w.ruleId}] ${w.message}`);
    }
  }
  if (result.claimsFlags.length > 0) {
    lines.push('');
    lines.push('Claims Flags:');
    for (const c of result.claimsFlags) {
      const icon = c.severity === 'critical' ? '🚫' : '⚠️';
      lines.push(`  ${icon} "${c.claim}" (${c.category}${c.platform ? ` — ${c.platform}` : ''})`);
    }
  }
  return lines.join('\n');
}
```

---

## 4. Backend Service + Controller + DTOs

### 4a. DTO: `generate-ad-creatives.dto.ts` (NEW)

**File:** `libraries/nestjs-libraries/src/dtos/ai-generate/generate-ad-creatives.dto.ts`

```typescript
import {
  IsString,
  IsOptional,
  IsArray,
  IsEnum,
  IsBoolean,
  MinLength,
  MaxLength,
  IsNumber,
  Min,
  Max,
} from 'class-validator';
import { Type } from 'class-transformer';

export class GenerateAdCreativesDto {
  @IsString()
  @MinLength(1)
  brandProfileId!: string;

  // Source: one of these must be provided
  @IsOptional()
  @IsString()
  contentIdeaId?: string;

  @IsOptional()
  @IsString()
  carouselProjectId?: string;

  // If no source, provide raw content
  @IsOptional()
  @IsString()
  @MaxLength(500)
  contentObjective?: string;  // e.g. "Promote our new course on AI"

  @IsOptional()
  @IsString()
  @MaxLength(200)
  productOrService?: string;  // e.g. "Online course on AI for marketers"

  // Target platforms
  @IsArray()
  @IsString({ each: true })
  platforms!: string[];  // ['META_FACEBOOK', 'META_INSTAGRAM', 'LINKEDIN']

  // Campaign objective
  @IsString()
  @IsEnum(['AWARENESS', 'CONSIDERATION', 'CONVERSION', 'LEAD_GENERATION', 'TRAFFIC', 'ENGAGEMENT'])
  objective!: string;

  // Ad type preference
  @IsString()
  @IsEnum(['STATIC', 'CAROUSEL', 'AUTO'])
  adType!: string;  // AUTO = let the AI decide based on objective

  // Optional template preference
  @IsOptional()
  @IsString()
  adTemplateId?: string;  // 'problem-solution', 'social-proof', etc.

  // Optional: generate N variants per platform
  @IsOptional()
  @Type(() => Number)
  @IsNumber()
  @Min(1)
  @Max(5)
  variants?: number;  // Default: 1

  // Destination URL
  @IsOptional()
  @IsString()
  @MaxLength(500)
  destinationUrl?: string;

  // Additional context
  @IsOptional()
  @IsString()
  @MaxLength(1000)
  additionalContext?: string;

  // CTA preference
  @IsOptional()
  @IsString()
  ctaButton?: string;  // 'LEARN_MORE', 'SHOP_NOW', etc.

  // Whether to include image generation prompts
  @IsOptional()
  @IsBoolean()
  generateImagePrompts?: boolean;  // Default: true
}

export class UpdateAdCreativeDto {
  @IsOptional()
  @IsString()
  headline?: string;

  @IsOptional()
  @IsString()
  primaryText?: string;

  @IsOptional()
  @IsString()
  description?: string;

  @IsOptional()
  @IsString()
  ctaButton?: string;

  @IsOptional()
  @IsString()
  destinationUrl?: string;

  @IsOptional()
  @IsString()
  status?: string;  // 'DRAFT', 'APPROVED', 'EXPORTED'
}

export class ExportAdCreativeDto {
  @IsString()
  adCreativeId!: string;

  @IsString()
  @IsEnum(['META_CSV', 'LINKEDIN_CSV', 'JSON', 'NATIVE_FORMAT'])
  format!: string;
}
```

### 4b. Schema Registration Update

**File:** `libraries/nestjs-libraries/src/ai-generate/schemas/index.ts` (MODIFY)

Add import and registry entry:

```typescript
// Add import:
import {
  AdCreativeBatchSchema,
  validate as _validateAdCreative,
  parse as _parseAdCreative,
} from './ad-creative.schema';

// Add re-export:
export { VERSION as AD_CREATIVE_VERSION, AdCreativeBatchSchema, validate as validateAdCreative, parse as parseAdCreative } from './ad-creative.schema';
export type { AdCreativeBatch, AdCreative } from './ad-creative.schema';

// Add to SCHEMA_REGISTRY:
'ad-creative': {
  name: 'Ad Creative',
  version: '1.0.0',
  schema: AdCreativeBatchSchema,
  validate: _validateAdCreative,
  parse: _parseAdCreative,
},
```

### 4c. AI Response Validator Update

**File:** `libraries/nestjs-libraries/src/ai-generate/ai-response-validator.ts` (MODIFY)

```typescript
// Add to SchemaType union:
| 'ad-creative'

// Add to SCHEMA_VERSIONS:
'ad-creative': AD_CREATIVE_VERSION,

// Add to SCHEMA_VALIDATORS:
'ad-creative': validateAdCreative,

// Add to getPromptVersion:
'ad-creative': '1.0.0',
```

### 4d. Service: `ad-creative-generate.service.ts` (NEW)

**File:** `libraries/nestjs-libraries/src/ai-generate/ad-creative-generate.service.ts`

```typescript
import { HttpException, HttpStatus, Injectable, Logger } from '@nestjs/common';
import { OpenaiService } from '@gitroom/nestjs-libraries/openai/openai.service';
import { BrandProfileService } from '@gitroom/nestjs-libraries/database/prisma/brands/brand-profile.service';
import { ContentIdeaService } from '@gitroom/nestjs-libraries/database/prisma/content-ideas/content-idea.service';
import { CarouselProjectService } from '@gitroom/nestjs-libraries/database/prisma/carousel-projects/carousel-project.service';
import { GenerationJobService } from '@gitroom/nestjs-libraries/database/prisma/generation-jobs/generation-job.service';
import { validateAiResponse, buildAiMetadata } from './ai-response-validator';
import { AdCreativeBatchSchema, type AdCreativeBatch, AD_PLATFORM_CONSTRAINTS } from './schemas/ad-creative.schema';
import { adTemplateRegistry } from './ad-templates/ad-template-registry';
import { runAdPolicyChecks } from './ad-templates/ad-policy-checker';
import { GenerateAdCreativesDto } from '@gitroom/nestjs-libraries/dtos/ai-generate/generate-ad-creatives.dto';
import { zodResponseFormat } from 'openai/helpers/zod';
import { z } from 'zod';
import { PrismaService } from '@gitroom/nestjs-libraries/database/prisma/prisma.service';

const SYSTEM_PROMPT = `You are a world-class performance marketing creative strategist working for ContentFlow.
You generate high-converting ad creatives for paid campaigns on Meta (Facebook/Instagram) and LinkedIn.

You MUST:
1. Generate ad copy that is distinct from organic social media content — this is PAID advertising
2. Respect strict character limits per platform:
   - Meta Feed: headline 125 chars, primary text 125 chars (recommended) / 500 max
   - Instagram Feed: headline 125 chars, primary text 125 chars / 500 max
   - LinkedIn: headline 70 chars, primary text 150 chars / 3000 max
3. Use platform-appropriate CTA buttons (LEARN_MORE, SHOP_NOW, SIGN_UP, etc.)
4. Follow ad copy best practices: benefit-driven, clear value proposition, strong CTA
5. Avoid any claims that violate ad policies:
   - No guaranteed results
   - No before/after body manipulation
   - No fake urgency or false scarcity
   - No misleading health claims
   - No superlatives without proof
6. Generate image prompts that match the ad's message and platform format
7. For carousel ads, ensure each slide has a clear purpose in the narrative flow
8. Include policy warnings for any potentially sensitive claims

AD TEMPLATE STRATEGIES:
- Problem→Solution: Hook with pain point, agitate, present solution, show benefit, CTA
- Social Proof: Lead with results/data, show testimonial, build trust, CTA
- Offer/Promotion: Lead with the offer, show value, create urgency, CTA
- Comparison/Before-After: Show current state, contrast with desired state, CTA
- Testimonial/Case: Feature customer voice, show results, CTA

IMPORTANT: These are NOT organic posts. They are PAID ADS. The language should be
direct, benefit-focused, and conversion-oriented. Every word must earn its place.`;

@Injectable()
export class AdCreativeGenerateService {
  private readonly logger = new Logger(AdCreativeGenerateService.name);

  constructor(
    private readonly brandProfileService: BrandProfileService,
    private readonly contentIdeaService: ContentIdeaService,
    private readonly carouselProjectService: CarouselProjectService,
    private readonly generationJobService: GenerationJobService,
    private readonly prisma: PrismaService,
    private readonly openaiService: OpenaiService,
  ) {}

  /**
   * Generate ad creatives for paid campaigns.
   */
  async generateAdCreatives(
    orgId: string,
    dto: GenerateAdCreativesDto
  ): Promise<AdCreativeBatch> {
    // 1. Validate brand ownership
    await this.brandProfileService.validateBrandOwnership(orgId, dto.brandProfileId);

    // 2. Load Brand DNA
    const brand = await this.brandProfileService.getBrand(dto.brandProfileId);
    const dna = await this.brandProfileService.getLatestDnaSnapshot(dto.brandProfileId);

    // 3. Load source content or use raw objective
    let sourceContent: {
      title: string;
      hook: string;
      goal: string;
      angle: string;
      slides?: any;
    } | null = null;

    if (dto.contentIdeaId) {
      const idea = await this.contentIdeaService.getIdea(dto.contentIdeaId);
      sourceContent = {
        title: idea.title,
        hook: idea.hook,
        goal: idea.goal,
        angle: idea.angle,
      };
    } else if (dto.carouselProjectId) {
      const project = await this.carouselProjectService.getProject(dto.carouselProjectId);
      sourceContent = {
        title: project.title,
        hook: '',
        goal: '',
        angle: project.caption || '',
        slides: project.slides,
      };
    } else if (dto.contentObjective) {
      sourceContent = {
        title: dto.contentObjective,
        hook: dto.contentObjective,
        goal: dto.objective,
        angle: dto.productOrService || '',
      };
    } else {
      throw new HttpException(
        'Either contentIdeaId, carouselProjectId, or contentObjective must be provided',
        HttpStatus.BAD_REQUEST
      );
    }

    // 4. Create generation job
    const job = await this.generationJobService.createJob({
      organizationId: orgId,
      brandProfileId: dto.brandProfileId,
      type: 'AD_CREATIVE_GENERATION',
      model: 'gpt-4.1',
      provider: 'openai',
      promptVersion: '1.0.0',
      schemaVersion: '1.0.0',
    });

    try {
      await this.generationJobService.startJob(job.id);

      // 5. Get ad template instructions
      let templateInstruction = '';
      if (dto.adTemplateId) {
        const template = adTemplateRegistry.get(dto.adTemplateId);
        if (template) {
          templateInstruction = template.promptInstruction;
        }
      }

      // 6. Build prompt
      const prompt = this.buildPrompt(dna, sourceContent, dto, templateInstruction);

      // 7. Call OpenAI with structured output
      const rawResponse = await this.openaiService.chat.completions.create({
        model: 'gpt-4.1',
        messages: [
          { role: 'system', content: SYSTEM_PROMPT },
          { role: 'user', content: prompt },
        ],
        response_format: zodResponseFormat(AdCreativeBatchSchema, 'adCreatives'),
      });

      const parsed = (rawResponse as any).choices[0]?.message?.parsed;
      if (!parsed) throw new Error('Failed to parse AI response');

      // 8. Validate with schema
      const validation = validateAiResponse('ad-creative', JSON.stringify(parsed));
      if (!validation.success) throw new Error('AI response validation failed');

      const batch = validation.data as AdCreativeBatch;

      // 9. Run policy checks on each ad
      for (const ad of batch.ads) {
        const policyResult = runAdPolicyChecks(ad, {
          forbiddenTerms: dna?.constraints?.avoid as string[],
          complianceNotes: dna?.constraints?.do as string[],
        });
        ad.policyWarnings = policyResult.warnings as any;
        ad.claimsFlags = ad.claimsFlags || [];
        // Merge claims flags from policy checker
        for (const flag of policyResult.claimsFlags) {
          ad.claimsFlags.push(flag as any);
        }
      }

      // 10. Complete job
      await this.generationJobService.completeJob(job.id, batch);

      return batch;
    } catch (error: any) {
      this.logger.error('Ad creative generation failed', error);
      await this.generationJobService.failJob(job.id, error.message);
      throw error;
    }
  }

  /**
   * Save generated ad creatives to the database.
   */
  async saveAdCreatives(
    orgId: string,
    batch: AdCreativeBatch,
    source: {
      brandProfileId: string;
      contentIdeaId?: string;
      carouselProjectId?: string;
    },
    jobId?: string
  ): Promise<any[]> {
    const saved = [];
    for (const ad of batch.ads) {
      const record = await this.prisma.adCreative.create({
        data: {
          organizationId: orgId,
          brandProfileId: source.brandProfileId,
          contentIdeaId: source.contentIdeaId || null,
          carouselProjectId: source.carouselProjectId || null,
          type: ad.type,
          platform: ad.platform as any,
          objective: ad.objective as any,
          adTemplateId: ad.adTemplateId || null,
          headline: ad.headline,
          primaryText: ad.primaryText,
          description: ad.description || null,
          ctaButton: ad.ctaButton,
          destinationUrl: ad.destinationUrl || null,
          slides: ad.slides ? JSON.parse(JSON.stringify(ad.slides)) : null,
          slideCount: ad.slideCount || (ad.slides ? ad.slides.length : null),
          policyWarnings: ad.policyWarnings ? JSON.parse(JSON.stringify(ad.policyWarnings)) : null,
          claimsFlags: ad.claimsFlags ? JSON.parse(JSON.stringify(ad.claimsFlags)) : null,
          mediaAssets: null,
          generationJobId: jobId || null,
          schemaVersion: '1.0.0',
          promptVersion: '1.0.0',
          status: 'DRAFT',
        },
      });
      saved.push(record);
    }
    return saved;
  }

  /**
   * Get ad creatives for an organization.
   */
  async getAdCreatives(
    orgId: string,
    filters?: {
      platform?: string;
      type?: string;
      status?: string;
      brandProfileId?: string;
    }
  ): Promise<any[]> {
    const where: any = {
      organizationId: orgId,
      deletedAt: null,
    };
    if (filters?.platform) where.platform = filters.platform;
    if (filters?.type) where.type = filters.type;
    if (filters?.status) where.status = filters.status;
    if (filters?.brandProfileId) where.brandProfileId = filters.brandProfileId;

    return this.prisma.adCreative.findMany({
      where,
      orderBy: { createdAt: 'desc' },
    });
  }

  /**
   * Get a single ad creative by ID.
   */
  async getAdCreativeById(orgId: string, id: string): Promise<any> {
    const ad = await this.prisma.adCreative.findFirst({
      where: { id, organizationId: orgId, deletedAt: null },
    });
    if (!ad) throw new HttpException('Ad creative not found', HttpStatus.NOT_FOUND);
    return ad;
  }

  /**
   * Update an ad creative.
   */
  async updateAdCreative(
    orgId: string,
    id: string,
    updates: Partial<{
      headline: string;
      primaryText: string;
      description: string;
      ctaButton: string;
      destinationUrl: string;
      status: string;
    }>
  ): Promise<any> {
    const existing = await this.getAdCreativeById(orgId, id);
    return this.prisma.adCreative.update({
      where: { id },
      data: updates,
    });
  }

  /**
   * Export ad creatives in platform-specific format.
   */
  async exportAdCreative(
    orgId: string,
    adCreativeId: string,
    format: 'META_CSV' | 'LINKEDIN_CSV' | 'JSON' | 'NATIVE_FORMAT'
  ): Promise<any> {
    const ad = await this.getAdCreativeById(orgId, adCreativeId);

    switch (format) {
      case 'META_CSV':
        return this.exportMetaCsv(ad);
      case 'LINKEDIN_CSV':
        return this.exportLinkedInCsv(ad);
      case 'JSON':
        return ad;
      case 'NATIVE_FORMAT':
        return this.exportNativeFormat(ad);
      default:
        throw new HttpException('Unsupported export format', HttpStatus.BAD_REQUEST);
    }
  }

  // ---- Private helpers ----

  private buildPrompt(
    dna: any,
    source: any,
    dto: GenerateAdCreativesDto,
    templateInstruction: string
  ): string {
    const platformConstraints = dto.platforms
      .map((p) => {
        const c = AD_PLATFORM_CONSTRAINTS[p];
        return c
          ? `- ${p}: headline max ${c.maxHeadline} chars, primary text max ${c.maxPrimaryText} chars, supported CTAs: ${c.supportedCtas.join(', ')}`
          : `- ${p}: (custom platform)`;
      })
      .join('\n');

    return `Generate ${dto.variants || 1} ad creative(s) for each of the following platforms: ${dto.platforms.join(', ')}

## Brand DNA
Brand Name: ${dna?.summary?.tagline || 'Unknown'}
Industry: ${dna?.summary?.industry || 'Unknown'}
Voice: ${dna?.voice?.tone || 'professional'} — Style: ${dna?.voice?.style || 'clear'}
Target Audience: ${dna?.audience?.demographics || 'General'}
Pain Points: ${Array.isArray(dna?.audience?.painPoints) ? dna.audience.painPoints.join(', ') : 'N/A'}
Value Proposition: ${Array.isArray(dna?.offer?.uniqueSellingPoints) ? dna.offer.uniqueSellingPoints.join(', ') : 'N/A'}
Forbidden Terms: ${Array.isArray(dna?.constraints?.avoid) ? dna.constraints.avoid.join(', ') : 'None'}
Compliance Notes: ${Array.isArray(dna?.constraints?.do) ? dna.constraints.do.join(', ') : 'None'}

## Source Content
Title: ${source.title}
Hook: ${source.hook}
Goal/Objective: ${dto.objective}
${source.angle ? `Angle: ${source.angle}` : ''}
${dto.productOrService ? `Product/Service: ${dto.productOrService}` : ''}

## Campaign Details
Objective: ${dto.objective}
Ad Type: ${dto.adType}
${dto.ctaButton ? `CTA Preference: ${dto.ctaButton}` : ''}
${dto.destinationUrl ? `Destination URL: ${dto.destinationUrl}` : ''}

## Platform Constraints
${platformConstraints}

## Ad Template Strategy
${templateInstruction || 'No specific template. Choose the best approach for the objective.'}

## Additional Context
${dto.additionalContext || 'None'}

IMPORTANT REMINDERS:
- These are PAID ADS, not organic social posts. Be direct and conversion-focused.
- Every ad needs: headline, primary text, CTA button
- Include imagePrompts with specific descriptions for ad images
- Run mental policy checks and include policyWarnings for anything sensitive
- For carousel type, generate 3-5 slides with clear narrative flow
- Each slide needs a headline and body text`;
  }

  private exportMetaCsv(ad: any): object {
    return {
      'Campaign Name': ad.headline,
      'Ad Name': `${ad.platform} - ${ad.type}`,
      'Headline': ad.headline,
      'Primary Text': ad.primaryText,
      'Description': ad.description || '',
      'Destination URL': ad.destinationUrl || '',
      'CTA': ad.ctaButton,
      'Status': 'Active',
    };
  }

  private exportLinkedInCsv(ad: any): object {
    return {
      'Campaign Name': ad.headline,
      'Ad Name': `${ad.platform} - ${ad.type}`,
      'Introduction': ad.headline,
      'Headline': ad.headline,
      'Description': ad.description || ad.primaryText,
      'Destination URL': ad.destinationUrl || '',
      'CTA': ad.ctaButton,
    };
  }

  private exportNativeFormat(ad: any): object {
    return {
      _format: 'contentflow-ad-v1',
      _exportedAt: new Date().toISOString(),
      ad,
    };
  }
}
```

### 4e. Controller: `ads.controller.ts` (NEW)

**File:** `apps/backend/src/api/routes/ads.controller.ts`

```typescript
import { Body, Controller, Delete, Get, Param, Patch, Post, Query } from '@nestjs/common';
import { ApiTags } from '@nestjs/swagger';
import { Organization } from '@prisma/client';
import { GetOrgFromRequest } from '@gitroom/nestjs-libraries/user/org.from.request';
import { BrandProfileService } from '@gitroom/nestjs-libraries/database/prisma/brands/brand-profile.service';
import { AdCreativeGenerateService } from '@gitroom/nestjs-libraries/ai-generate/ad-creative-generate.service';
import {
  GenerateAdCreativesDto,
  UpdateAdCreativeDto,
  ExportAdCreativeDto,
} from '@gitroom/nestjs-libraries/dtos/ai-generate/generate-ad-creatives.dto';

@ApiTags('Ads')
@Controller('/ads')
export class AdsController {
  constructor(
    private readonly adCreativeGenerateService: AdCreativeGenerateService,
    private readonly brandProfileService: BrandProfileService,
  ) {}

  // ---- Ad Templates ----

  @Get('/templates')
  getAdTemplates(
    @Query('category') category?: string,
    @Query('objective') objective?: string,
    @Query('platform') platform?: string,
  ) {
    // Delegate to adTemplateRegistry — imported from service or injected
    const { adTemplateRegistry } = require('@gitroom/nestjs-libraries/ai-generate/ad-templates/ad-template-registry');
    if (category) return adTemplateRegistry.getByCategory(category);
    if (objective) return adTemplateRegistry.getByObjective(objective);
    if (platform) return adTemplateRegistry.getByPlatform(platform);
    return adTemplateRegistry.getActive();
  }

  @Get('/templates/summary')
  getAdTemplatesSummary() {
    const { adTemplateRegistry } = require('@gitroom/nestjs-libraries/ai-generate/ad-templates/ad-template-registry');
    return adTemplateRegistry.getSummary();
  }

  @Get('/templates/:id')
  getAdTemplateById(@Param('id') id: string) {
    const { adTemplateRegistry } = require('@gitroom/nestjs-libraries/ai-generate/ad-templates/ad-template-registry');
    const template = adTemplateRegistry.get(id);
    if (!template) return { error: 'Template not found', id };
    return template;
  }

  // ---- Generate Ad Creatives ----

  @Post('/generate')
  async generateAds(
    @GetOrgFromRequest() org: Organization,
    @Body() body: GenerateAdCreativesDto,
  ) {
    if (body.brandProfileId) {
      await this.brandProfileService.validateBrandOwnership(org.id, body.brandProfileId);
    }
    return this.adCreativeGenerateService.generateAdCreatives(org.id, body);
  }

  // ---- Save Generated Ads ----

  @Post('/save')
  async saveAds(
    @GetOrgFromRequest() org: Organization,
    @Body() body: {
      ads: any; // AdCreativeBatch
      brandProfileId: string;
      contentIdeaId?: string;
      carouselProjectId?: string;
      generationJobId?: string;
    },
  ) {
    return this.adCreativeGenerateService.saveAdCreatives(
      org.id,
      body.ads,
      {
        brandProfileId: body.brandProfileId,
        contentIdeaId: body.contentIdeaId,
        carouselProjectId: body.carouselProjectId,
      },
      body.generationJobId,
    );
  }

  // ---- CRUD ----

  @Get('/')
  async listAds(
    @GetOrgFromRequest() org: Organization,
    @Query('platform') platform?: string,
    @Query('type') type?: string,
    @Query('status') status?: string,
    @Query('brandProfileId') brandProfileId?: string,
  ) {
    return this.adCreativeGenerateService.getAdCreatives(org.id, {
      platform,
      type,
      status,
      brandProfileId,
    });
  }

  @Get('/:id')
  async getAd(
    @GetOrgFromRequest() org: Organization,
    @Param('id') id: string,
  ) {
    return this.adCreativeGenerateService.getAdCreativeById(org.id, id);
  }

  @Patch('/:id')
  async updateAd(
    @GetOrgFromRequest() org: Organization,
    @Param('id') id: string,
    @Body() body: UpdateAdCreativeDto,
  ) {
    return this.adCreativeGenerateService.updateAdCreative(org.id, id, body);
  }

  // ---- Export ----

  @Post('/export')
  async exportAd(
    @GetOrgFromRequest() org: Organization,
    @Body() body: ExportAdCreativeDto,
  ) {
    return this.adCreativeGenerateService.exportAdCreative(
      org.id,
      body.adCreativeId,
      body.format as any,
    );
  }
}
```

### 4f. API Module Registration (MODIFY)

**File:** `apps/backend/src/api/api.module.ts`

Add to imports:
```typescript
import { AdsController } from '@gitroom/backend/api/routes/ads.controller';
import { AdCreativeGenerateService } from '@gitroom/nestjs-libraries/ai-generate/ad-creative-generate.service';
import { PrismaService } from '@gitroom/nestjs-libraries/database/prisma/prisma.service';
```

Add to `authenticatedController` array:
```typescript
AdsController,
```

Add to `providers` array:
```typescript
AdCreativeGenerateService,
PrismaService,
```

---

## 5. Frontend Page and Components

### 5a. Types: `ads.types.ts` (NEW)

**File:** `apps/frontend/src/components/ads/ads.types.ts`

```typescript
export type AdPlatform = 'META_FACEBOOK' | 'META_INSTAGRAM' | 'LINKEDIN';
export type AdCreativeType = 'STATIC' | 'CAROUSEL';
export type AdObjective = 'AWARENESS' | 'CONSIDERATION' | 'CONVERSION' | 'LEAD_GENERATION' | 'TRAFFIC' | 'ENGAGEMENT';
export type AdCreativeStatus = 'DRAFT' | 'APPROVED' | 'EXPORTED';

export interface AdTemplateSummary {
  id: string;
  label: string;
  labelEn: string;
  description: string;
  category: string;
  objective: string[];
  active: boolean;
  preferredPlatforms: string[];
}

export interface GeneratedAdCreative {
  type: AdCreativeType;
  platform: AdPlatform;
  objective: AdObjective;
  adTemplateId?: string;
  headline: string;
  primaryText: string;
  description?: string;
  ctaButton: string;
  destinationUrl?: string;
  slides?: AdCarouselSlide[];
  slideCount?: number;
  imagePrompts?: AdImagePrompt[];
  policyWarnings: PolicyWarning[];
  claimsFlags: ClaimFlag[];
  tone: string;
  rationale?: string;
  notes?: string;
}

export interface AdCarouselSlide {
  index: number;
  headline: string;
  body: string;
  imageUrl?: string;
  cta?: string;
  altText?: string;
}

export interface AdImagePrompt {
  role: string;
  prompt: string;
  aspectRatio?: string;
}

export interface PolicyWarning {
  ruleId: string;
  severity: 'info' | 'warning' | 'critical';
  message: string;
  suggestion?: string;
  category: string;
}

export interface ClaimFlag {
  claim: string;
  severity: 'info' | 'warning' | 'critical';
  category: string;
  platform?: string;
}

export interface AdCreativeBatch {
  ads: GeneratedAdCreative[];
}

export interface GenerateAdsParams {
  brandProfileId: string;
  contentIdeaId?: string;
  carouselProjectId?: string;
  contentObjective?: string;
  productOrService?: string;
  platforms: AdPlatform[];
  objective: AdObjective;
  adType: AdCreativeType | 'AUTO';
  adTemplateId?: string;
  variants?: number;
  destinationUrl?: string;
  additionalContext?: string;
  ctaButton?: string;
  generateImagePrompts?: boolean;
}

export interface SavedAdCreative {
  id: string;
  type: AdCreativeType;
  platform: AdPlatform;
  objective: AdObjective;
  headline: string;
  primaryText: string;
  description?: string;
  ctaButton: string;
  destinationUrl?: string;
  status: AdCreativeStatus;
  policyWarnings?: any;
  claimsFlags?: any;
  createdAt: string;
}
```

### 5b. API Service: `ads.service.ts` (NEW)

**File:** `apps/frontend/src/components/ads/ads.service.ts`

```typescript
import { loadVars } from '@gitroom/react/helpers/variable.context';
import type { GenerateAdsParams, AdCreativeBatch, SavedAdCreative } from './ads.types';

const BASE = '/ads';

async function api(path: string, options?: RequestInit) {
  const { backendUrl } = loadVars();
  const res = await fetch(backendUrl + path, {
    credentials: 'include',
    ...options,
    headers: {
      Accept: 'application/json',
      'Content-Type': 'application/json',
      ...options?.headers,
    },
  });
  if (!res.ok) {
    const err = await res.json().catch(() => ({ message: res.statusText }));
    throw new Error(err.message || err.error || 'API error');
  }
  return res.json();
}

export async function generateAds(data: GenerateAdsParams): Promise<AdCreativeBatch> {
  return api(BASE + '/generate', { method: 'POST', body: JSON.stringify(data) });
}

export async function saveAds(data: {
  ads: AdCreativeBatch;
  brandProfileId: string;
  contentIdeaId?: string;
  carouselProjectId?: string;
}): Promise<SavedAdCreative[]> {
  return api(BASE + '/save', { method: 'POST', body: JSON.stringify(data) });
}

export async function listAds(filters?: {
  platform?: string;
  type?: string;
  status?: string;
  brandProfileId?: string;
}): Promise<SavedAdCreative[]> {
  const params = new URLSearchParams();
  if (filters?.platform) params.set('platform', filters.platform);
  if (filters?.type) params.set('type', filters.type);
  if (filters?.status) params.set('status', filters.status);
  if (filters?.brandProfileId) params.set('brandProfileId', filters.brandProfileId);
  const qs = params.toString();
  return api(BASE + (qs ? `?${qs}` : ''));
}

export async function getAd(id: string): Promise<SavedAdCreative> {
  return api(`${BASE}/${id}`);
}

export async function updateAd(id: string, updates: Partial<SavedAdCreative>): Promise<SavedAdCreative> {
  return api(`${BASE}/${id}`, { method: 'PATCH', body: JSON.stringify(updates) });
}

export async function exportAd(adCreativeId: string, format: string): Promise<any> {
  return api(BASE + '/export', { method: 'POST', body: JSON.stringify({ adCreativeId, format }) });
}

export async function getAdTemplates(params?: {
  category?: string;
  objective?: string;
  platform?: string;
}) {
  const qs = new URLSearchParams(params as any).toString();
  return api(`${BASE}/templates${qs ? `?${qs}` : ''}`);
}

export async function getAdTemplatesSummary() {
  return api(`${BASE}/templates/summary`);
}
```

### 5c. SWR Hooks: `ads.hooks.ts` (NEW)

**File:** `apps/frontend/src/components/ads/ads.hooks.ts`

```typescript
import useSWR from 'swr';
import { listAds, getAdTemplatesSummary } from './ads.service';
import type { SavedAdCreative, AdTemplateSummary } from './ads.types';

export function useAdCreatives(filters?: { platform?: string; type?: string; status?: string }) {
  const key = filters
    ? `/ads?${new URLSearchParams(filters as any).toString()}`
    : '/ads';
  return useSWR<SavedAdCreative[]>(key, () => listAds(filters));
}

export function useAdTemplates() {
  return useSWR<AdTemplateSummary[]>('/ads/templates/summary', getAdTemplatesSummary);
}
```

### 5d. Frontend Components

#### `ads-page.component.tsx` (NEW)
**File:** `apps/frontend/src/components/ads/ads-page.component.tsx`

Main page component with:
- Ad template selector (carousel of template cards)
- Configuration form (platform, objective, ad type, source selection)
- Generation trigger button
- Generated ads preview grid
- Policy warnings panel
- Export buttons (Meta CSV, LinkedIn CSV, JSON)
- Saved ads list with filters

#### `ad-template-card.component.tsx` (NEW)
**File:** `apps/frontend/src/components/ads/ad-template-card.component.tsx`

Card component showing:
- Template name and description
- Category badge (conversion, proof, offer)
- Platform icons
- Slide structure preview
- Select button

#### `ad-preview.component.tsx` (NEW)
**File:** `apps/frontend/src/components/ads/ad-preview.component.tsx`

Platform-specific ad preview:
- Meta feed ad preview (headline + image + CTA button)
- Instagram feed ad preview
- LinkedIn ad preview
- Static vs carousel mode (swipeable slides)

#### `ad-policy-panel.component.tsx` (NEW)
**File:** `apps/frontend/src/components/ads/ad-policy-panel.component.tsx`

Policy compliance panel:
- Severity badges (critical, warning, info)
- Expandable details per warning
- Category icons
- Suggested fixes

#### `ad-export-dialog.component.tsx` (NEW)
**File:** `apps/frontend/src/components/ads/ad-export-dialog.component.tsx`

Export dialog with:
- Format selection (Meta CSV, LinkedIn CSV, JSON)
- Preview of export format
- Download button
- Copy to clipboard option

### 5e. Next.js Route (NEW)

**File:** `apps/frontend/src/app/(app)/(site)/ads/page.tsx`

```tsx
import { Metadata } from 'next';
import { isGeneralServerSide } from '@gitroom/helpers/utils/is.general.server.side';
import { AdsPage } from '@gitroom/frontend/components/ads/ads-page.component';

export const dynamic = 'force-dynamic';

export const metadata: Metadata = {
  title: `${isGeneralServerSide() ? 'ContentFlow' : 'Gitroom'} Ads Generator`,
  description: 'Generate ad creatives for paid campaigns',
};

export default async function AdsRoute() {
  return <AdsPage />;
}
```

---

## 6. API Endpoints Summary

| Method | Path | Description | Request Body | Response |
|--------|------|-------------|--------------|----------|
| `GET` | `/ads/templates` | List ad templates | Query: `category?`, `objective?`, `platform?` | `AdTemplateDefinition[]` |
| `GET` | `/ads/templates/summary` | Get template summaries | — | `AdTemplateSummary[]` |
| `GET` | `/ads/templates/:id` | Get template by ID | — | `AdTemplateDefinition` |
| `POST` | `/ads/generate` | Generate AI ad creatives | `GenerateAdCreativesDto` | `AdCreativeBatch` |
| `POST` | `/ads/save` | Save generated ads to DB | `{ ads, brandProfileId, contentIdeaId?, ... }` | `AdCreative[]` |
| `GET` | `/ads/` | List saved ads | Query: `platform?`, `type?`, `status?`, `brandProfileId?` | `AdCreative[]` |
| `GET` | `/ads/:id` | Get single ad creative | — | `AdCreative` |
| `PATCH` | `/ads/:id` | Update ad creative | `UpdateAdCreativeDto` | `AdCreative` |
| `POST` | `/ads/export` | Export ad in platform format | `ExportAdCreativeDto` | Export data (CSV/JSON) |

---

## 7. Ad Policy Check Logic (Summary)

The policy check system operates at **two levels**:

### Level 1: AI Prompt Instructions (Pre-generation)
The system prompt includes policy guidelines:
- No guaranteed results
- No before/after body manipulation
- No fake urgency
- No misleading health claims
- Platform-specific character limits

### Level 2: Post-generation Policy Checker
After the AI generates the ad, `runAdPolicyChecks()` runs:

1. **Template-specific checks** — Each template defines checks relevant to its strategy (e.g., social-proof template checks for fake testimonials)
2. **Platform-specific checks** — Meta has stricter health/beauty restrictions
3. **Generic restrictions** — Common ad policy violations (guarantees, fake urgency, superlatives)
4. **Brand constraint checks** — Verifies forbidden terms from Brand DNA aren't used
5. **Format checks** — Carousel slide count, character limits per platform

Results are stored on the `AdCreative` record as `policyWarnings` and `claimsFlags` JSON fields.

---

## 8. Build Order (Step-by-Step)

### Phase 1: Prisma Schema & Migration (Day 1)

| Step | Action | File |
|------|--------|------|
| 1 | Add `AD_CREATIVE_GENERATION` to `GenerationJobType` enum | `schema.prisma` |
| 2 | Add `AdCreative` model with all fields and enums | `schema.prisma` |
| 3 | Add `adCreatives` relation to `ContentIdea`, `CarouselProject`, `BrandProfile` | `schema.prisma` |
| 4 | Run migration | `npx prisma migrate dev --name add-ad-creative-model` |

### Phase 2: AI Schema & Validation (Day 1-2)

| Step | Action | File |
|------|--------|------|
| 5 | Create `ad-creative.schema.ts` with Zod schemas | `ai-generate/schemas/ad-creative.schema.ts` |
| 6 | Register in `schemas/index.ts` (add to SCHEMA_REGISTRY) | `ai-generate/schemas/index.ts` |
| 7 | Update `ai-response-validator.ts` (add `ad-creative` type) | `ai-generate/ai-response-validator.ts` |

### Phase 3: Ad Templates & Policy (Day 2)

| Step | Action | File |
|------|--------|------|
| 8 | Create `ad-template-definitions.ts` with 5 templates | `ai-generate/ad-templates/ad-template-definitions.ts` |
| 9 | Create `ad-template-registry.ts` | `ai-generate/ad-templates/ad-template-registry.ts` |
| 10 | Create `ad-policy-checker.ts` | `ai-generate/ad-templates/ad-policy-checker.ts` |

### Phase 4: Backend Service & Controller (Day 2-3)

| Step | Action | File |
|------|--------|------|
| 11 | Create `generate-ad-creatives.dto.ts` | `dtos/ai-generate/generate-ad-creatives.dto.ts` |
| 12 | Create `ad-creative-generate.service.ts` | `ai-generate/ad-creative-generate.service.ts` |
| 13 | Create `ads.controller.ts` | `apps/backend/src/api/routes/ads.controller.ts` |
| 14 | Register controller + service in `api.module.ts` | `apps/backend/src/api/api.module.ts` |

### Phase 5: Frontend (Day 3-4)

| Step | Action | File |
|------|--------|------|
| 15 | Create `ads.types.ts` | `apps/frontend/src/components/ads/ads.types.ts` |
| 16 | Create `ads.service.ts` | `apps/frontend/src/components/ads/ads.service.ts` |
| 17 | Create `ads.hooks.ts` | `apps/frontend/src/components/ads/ads.hooks.ts` |
| 18 | Create `ad-template-card.component.tsx` | `apps/frontend/src/components/ads/ad-template-card.component.tsx` |
| 19 | Create `ad-preview.component.tsx` | `apps/frontend/src/components/ads/ad-preview.component.tsx` |
| 20 | Create `ad-policy-panel.component.tsx` | `apps/frontend/src/components/ads/ad-policy-panel.component.tsx` |
| 21 | Create `ad-export-dialog.component.tsx` | `apps/frontend/src/components/ads/ad-export-dialog.component.tsx` |
| 22 | Create `ads-page.component.tsx` | `apps/frontend/src/components/ads/ads-page.component.tsx` |
| 23 | Create `ads/page.tsx` (route) | `apps/frontend/src/app/(app)/(site)/ads/page.tsx` |

### Phase 6: UI Integration (Day 4)

| Step | Action | File |
|------|--------|------|
| 24 | Add "Generate Ads" button to content-swipe page | `content-swipe-page.component.tsx` |
| 25 | Add "Generate Ads" button to carousel project detail | `carousel-project-page.component.tsx` |
| 26 | Add ads tab/view to `/launches` area (optional) | `launches/` |

### Phase 7: Testing & Polish (Day 5)

| Step | Action |
|------|--------|
| 27 | Unit tests for `AdCreativeGenerateService` |
| 28 | Unit tests for `ad-policy-checker.ts` |
| 29 | Integration tests for controller endpoints |
| 30 | E2E test: Idea → Generate Ads → Save → Export |
| 31 | Character limit validation per platform |
| 32 | Error handling for AI failures |
| 33 | Policy warning display in UI |

---

## 9. Error Handling & Edge Cases

| Scenario | Handling |
|----------|----------|
| Brand DNA not found | Return 404 with clear message |
| Content idea not found | Return 404 |
| No source provided (no idea, no carousel, no objective) | Return 400 Bad Request |
| AI response exceeds char limit | Auto-truncate with `...` suffix + policy warning |
| AI returns invalid JSON | Use `validateAiResponse` with repair attempts |
| AI generation fails | Mark job as FAILED, return error to user |
| Policy violations detected | Store warnings on AdCreative, return in response — do NOT block save |
| Carousel with < 2 slides | Policy warning + frontend validation |
| LinkedIn carousel > 5 slides | Policy warning + frontend validation |
| Invalid export format | Return 400 Bad Request |
| Template not found | Ignore template, use generic ad strategy |

---

## 10. Key Differentiators: Ads vs Organic Carousels

| Aspect | Organic Carousel | Ad Creative |
|--------|-----------------|-------------|
| **Purpose** | Educate, engage, build community | Convert, drive action, generate leads |
| **Structure** | Slide-by-slide narrative | Headline + Primary Text + CTA + Image |
| **CTA** | "Save this post", "Share with a friend" | "Shop Now", "Sign Up", "Learn More" |
| **Copy** | Conversational, value-driven | Direct, benefit-focused, conversion-oriented |
| **Fields** | title, slides[], caption, hashtags | headline, primaryText, ctaButton, destinationUrl |
| **Platform format** | Native post format | Ad format (feed, stories, sidebar) |
| **Compliance** | Basic brand checks | Full ad policy checks (Meta, LinkedIn policies) |
| **Destination** | Organic feed | Paid ad placement with tracking URL |
| **Template** | Educational, storytelling, list, etc. | Problem→Solution, Offer, Proof, Comparison, Testimonial |

---

## 11. Files Summary

| # | File | Action | Purpose |
|---|------|--------|---------|
| 1 | `schema.prisma` | MODIFY | Add AD_CREATIVE_GENERATION enum, AdCreative model, relations |
| 2 | `ai-generate/schemas/ad-creative.schema.ts` | CREATE | Zod schema for AI output |
| 3 | `ai-generate/schemas/index.ts` | MODIFY | Register ad-creative schema |
| 4 | `ai-generate/ai-response-validator.ts` | MODIFY | Add ad-creative to validator |
| 5 | `ai-generate/ad-templates/ad-template-definitions.ts` | CREATE | 5 ad template data definitions |
| 6 | `ai-generate/ad-templates/ad-template-registry.ts` | CREATE | Template registry with query methods |
| 7 | `ai-generate/ad-templates/ad-policy-checker.ts` | CREATE | Policy check logic |
| 8 | `dtos/ai-generate/generate-ad-creatives.dto.ts` | CREATE | Request DTOs |
| 9 | `ai-generate/ad-creative-generate.service.ts` | CREATE | Core generation service |
| 10 | `apps/backend/src/api/routes/ads.controller.ts` | CREATE | REST endpoints |
| 11 | `apps/backend/src/api/api.module.ts` | MODIFY | Register controller + service |
| 12 | `apps/frontend/src/components/ads/ads.types.ts` | CREATE | TypeScript types |
| 13 | `apps/frontend/src/components/ads/ads.service.ts` | CREATE | API client |
| 14 | `apps/frontend/src/components/ads/ads.hooks.ts` | CREATE | SWR hooks |
| 15 | `apps/frontend/src/components/ads/ad-template-card.component.tsx` | CREATE | Template selection card |
| 16 | `apps/frontend/src/components/ads/ad-preview.component.tsx` | CREATE | Platform-specific ad preview |
| 17 | `apps/frontend/src/components/ads/ad-policy-panel.component.tsx` | CREATE | Policy warnings panel |
| 18 | `apps/frontend/src/components/ads/ad-export-dialog.component.tsx` | CREATE | Export dialog |
| 19 | `apps/frontend/src/components/ads/ads-page.component.tsx` | CREATE | Main page component |
| 20 | `apps/frontend/src/app/(app)/(site)/ads/page.tsx` | CREATE | Next.js route |

**Total: 13 new files, 7 modified files**
